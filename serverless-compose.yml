org: 200millas
service: my-multi-service-project

provider:
  name: aws
  runtime: python3.13
  region: us-east-1
  memorySize: 256
  timeout: 20

services:
  service-resources:
    path: resources
  service-employees:
    path: employees
    dependsOn:
      - service-resources
  service-users:
    path: users
    dependsOn:
      - service-resources
  service-products:
    path: products
    dependsOn:
      - service-resources
